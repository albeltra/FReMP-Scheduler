const tent_types = {
    TENT_TYPES
}

const dont_summarize = {
  DONT_SUMMARIZE
};

const no_sum = {
  NO_SUM
};


const sum_keys = {  
  SUM_KEYS
}

const historical = [
  HISTORICAL_KEYS
]


export {historical, tent_types, dont_summarize, no_sum,sum_keys};